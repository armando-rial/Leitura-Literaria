import { FiltroPesquisaPipe } from './filtro-pesquisaLeitura-pipe';

describe('FiltroPesquisaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPesquisaPipe();
    expect(pipe).toBeTruthy();
  });
});
